<p class="text-2xl p-4 text-center font-bold bg-gray-800 m-2 text-white rounded-lg">Who is The AI?</p>
<label for="prompt" class="text-sm p-2 font-semibold">Prompt</label>
<p id="prompt" class="p-4 bg-zinc-300 font-semibold m-2 rounded-lg">Describe a futuristic cityscape where technology and nature coexist in harmony.</p>

<label for="responses" class="text-sm p-2 font-semibold">Responses</label>
<div id="responses" class="m-2">
    <% for(let i = 0; i < players.length; i++) { %>
        <!-- FORMAT A LIST OF JSON {alias: NAME_HERE, aliasPicture: SRC_HERE, response: CONTENT_HERE} -->
        <%- include("./templates/responseCard", {playerName: players[i].alias, playerAvatar: players[i].aliasPicture, responseContent: players[i].response}) %>
    <% } %>
</div>